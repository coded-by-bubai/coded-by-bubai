name: Update README with Repo Cards

on:
  schedule:
    - cron: "0 0 * * 1"  # Every Monday at 00:00 UTC
  workflow_dispatch:     # Allows manual trigger

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main  # Change to 'master' or your main branch name if different

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install requests
        run: pip install requests

      - name: Run script to update README
        run: python .github/scripts/generate_repo_cards.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # Check for changes and commit only if needed
          if git diff --quiet && git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git add README.md
            git commit -m "🤖 Update repo cards in README"
            git push origin HEAD:main
          fi
